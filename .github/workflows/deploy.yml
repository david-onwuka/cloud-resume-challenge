name: Cloud Resume Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Deploy Terraform Infrastructure
        run: |
          terraform init
          terraform apply -auto-approve

      - name: Create Frontend Folder
        run: |
          mkdir -p frontend
          echo "<!DOCTYPE html><html><head><title>My Resume</title></head><body><h1>Cloud Resume Challenge</h1><p>Website coming soon!</p></body></html>" > frontend/index.html
          echo "body { font-family: Arial; margin: 40px; }" > frontend/style.css

      - name: Deploy Frontend to S3
        run: |
          aws s3 sync ./frontend s3://david-cloud-resume-challenge2025 --delete
